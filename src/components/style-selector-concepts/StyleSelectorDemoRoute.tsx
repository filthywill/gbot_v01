import React from 'react';
import { StyleSelectorShowcase } from './StyleSelectorShowcase';

/**
 * Demo component to display the style selector showcase
 * Can be included in a router setup
 */
export const StyleSelectorDemoRoute: React.FC = () => {
  return <StyleSelectorShowcase />;
};

export default StyleSelectorDemoRoute; 